var crypto = require('crypto');
// make a cryptographic hash of a salt and a password and store them.
var cryptic = function(data){
    var hash = crypto.createHash('sha1')
    hash.update(data)
    var jungle = hash.digest('hex')
    return jungle
}
module.exports = cryptic;